---

tower_organization: "james-group"
tower_projects:
   name: "Kubernetes"
   description: "tower project pointing to Image factory github repo"
   organization: "james-group"
   scm_type: "git"
   scm_url: "https://github.devops.worldpay.local/robops/iac-k8s-service.git"
   scm_branch: "master"

tower_inventories:
  - name: BBE
    description: Test
    organization: "{{ tower_organization }}"
    hosts:
      name: test
      descrtiption: test


tower_job_templates:
  - name: a
    description: descrip
    inventory: "{{ tower_inventory['name'] }}"
    project: "{{ tower_project['name'] }}"
    extra_vars_path: "{{  }}"
    playbook: "{{  }}"
    job_tags: "{{  }}"
    vault_credential: "{{  }}"